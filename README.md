# money-manager

What you need to do?

-   Activate extension intl in php.ini
-   Run: composer update
-   Setting env
-   Migrate DB and run seeder
    php artisan migrate --seeder
-   Run: php artisan key:generate
-   Run: php artisan storage:link

What you need to know?

-   config/custom.php : used to store custom params
